#include <stdio.h>
#include <stdlib.h>
#include "heapManager.h"

heap *todebug;

typedef struct info{
     int roll;
     char *a;
}info;


int main(){   
     
    char *p, *r, *s;
    
    p = (char*)my_malloc(28);

 // my_free(q);
//    s = (char *)my_malloc(100);

//    my_free(p);
//    my_free(r);
//    my_free(s);

   
}